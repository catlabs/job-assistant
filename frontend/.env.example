# Built into the static bundle at build time. Point this at the public backend URL
# before running `npm run build`, then rebuild whenever a VITE_* value changes.
VITE_API_BASE_URL=http://127.0.0.1:8000
# Optional light demo gate only. This value is visible in the browser bundle.
VITE_API_KEY=
